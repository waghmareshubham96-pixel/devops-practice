pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage running'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'exit 1'
            }
        }

        stage('Run App') {
            steps {
                echo 'Run app stage running'
            }
        }
    }
}
